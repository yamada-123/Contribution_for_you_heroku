<h2>コメントを編集する</h2>
<%= form_with(model: [@supply,@comment_supply],local: true) do |form| %>

  <div class = "field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div class = "actions">
    <%= form.submit %>
  </div>
<% end %>